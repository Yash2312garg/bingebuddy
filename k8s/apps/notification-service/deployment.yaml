apiVersion: v1
kind: ConfigMap
metadata:
  name: notification-config
  namespace: bingebuddy-dev
  labels:
    app: notification-service
    app.kubernetes.io/managed-by: argocd
data:
  NODE_ENV: "staging"
  PORT: "3001"
  DB_HOST: "notification-postgres"
  DB_PORT: "5432"
  DB_NAME: "notifications"
  DB_USER: "admin"
  RABBITMQ_URL: "amqp://yash:Yash@123@rabbitmq:5672"
  FRONTEND_URL: "http://bingebuddy.local:8080"
---
apiVersion: v1
kind: Secret
metadata:
  name: notification-secrets
  namespace: bingebuddy-dev
  labels:
    app: notification-service
    app.kubernetes.io/managed-by: argocd
type: Opaque
data:
  # echo -n "admin123" | base64
  DB_PASSWORD: YWRtaW4xMjM=
  # echo -n "v5tmmf2ennoca7hv@ethereal.email" | base64
  NODEMAILER_USER: djV0bW1mMmVubm9jYTdodkBldGhlcmVhbC5lbWFpbA==
  # echo -n "Ffzj4GPA7SGaagWWDH" | base64
  NODEMAILER_USER_PASSWORD: RmZ6ajRHUEE3U0dhYWdXV0RI
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: bingebuddy-dev
  labels:
    app: notification-service
    app.kubernetes.io/managed-by: argocd
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      terminationGracePeriodSeconds: 30
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: notification-service
          # ⚠️ GitHub Actions CI updates this tag automatically
          image: ghcr.io/yash2312garg/bingebuddy-notification-service:511eb3e
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 3001
              protocol: TCP
          env:
            - name: POSTGRES_USER
              valueFrom:
                configMapKeyRef:
                  name: notification-config
                  key: DB_USER
            - name: POSTGRES_DB
              valueFrom:
                configMapKeyRef:
                  name: notification-config
                  key: DB_NAME
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: notification-secrets
                  key: DB_PASSWORD
          envFrom:
            - configMapRef:
                name: notification-config
            - secretRef:
                name: notification-secrets
          readinessProbe:
            httpGet:
              path: /health
              port: 3001
            initialDelaySeconds: 15
            periodSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 3001
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 3
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          securityContext:
            runAsNonRoot: true
            runAsUser: 1001
            allowPrivilegeEscalation: false
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: bingebuddy-dev
  labels:
    app: notification-service
    app.kubernetes.io/managed-by: argocd
spec:
  type: ClusterIP
  selector:
    app: notification-service
  ports:
    - name: http
      port: 3001
      targetPort: 3001
      protocol: TCP
